overwrite: true
schema:
  - "http://localhost:5050/query":
      headers:
        Authorization: "xxxxxxxx"
generates:
  ./gql-types.d.ts:
    plugins:
      - typescript
      - typescript-operations
